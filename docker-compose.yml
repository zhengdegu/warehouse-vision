version: "3.8"

services:
  warehouse-vision:
    build: .
    container_name: warehouse-vision
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./configs:/app/configs
      - ./data:/app/data
      - ./events:/app/events
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,utility
      - TZ=Asia/Shanghai
    # GPU 支持（需要 nvidia-container-toolkit）
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
